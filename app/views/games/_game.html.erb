<%= link_to "#{game.name}", game %>
<% if game.average_rating.nan? %>
	This game is not yet rated. 
<% else %>
	average rating: <%= game.average_rating %> 
<% end %>
<br>
<% comment = game.comments.count %>
<% if comment %>
  <%= pluralize(comment, 'comment') %>
<% end %>